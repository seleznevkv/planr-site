# Тяжёлое и локальное — пересобирается внутри образа (npm ci + vite build).
node_modules
dist

# git и IDE
.git
.gitignore
.vscode
.claude

# мусор ОС и логи
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env-файлы не бейкаем в образ
.env*
