# Тяжёлое и локальное — пересобирается внутри образа (npm ci + next build).
node_modules
.next
out
build

# отдельное приложение со своим Dockerfile — не часть сборки сайта
rostpro-guide

# git и IDE
.git
.gitignore
.vscode
.claude

# мусор ОС и логи
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env-файлы не бейкаем в образ
.env*

# typescript build info
*.tsbuildinfo
