# Тяжёлое и локальное — пересобирается внутри образа (npm ci + next build).
node_modules
.next
out
build

# git и IDE
.git
.gitignore
.vscode
.claude

# мусор ОС и логи
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env-файлы не бейкаем в образ
.env*

# typescript build info
*.tsbuildinfo
