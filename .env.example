# SMTP settings for app/api/contact/route.ts (contact form notification email).
# Copy to .env.local for local dev and fill in real values.
SMTP_HOST=smtp.mail.ru
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=info@rostpro.com
SMTP_PASS=
CONTACT_EMAIL_TO=office@rostpro.com
